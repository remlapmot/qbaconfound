% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/confounders.R
\name{u_continuous}
\alias{u_continuous}
\title{Specify a continuous unmeasured confounder}
\usage{
u_continuous(coef_out, coef_exp, resid_sd, resid_dist = c("uniform", "gamma"))
}
\arguments{
\item{coef_out}{Length-2 numeric \code{c(mean, sd)} giving the normal prior for
the coefficient of the unmeasured confounder in the outcome model
(\eqn{\beta_U}). Set \code{sd = 0} for a point-mass (fixed value) prior.}

\item{coef_exp}{Normal prior for the coefficient(s) of the exposure in the
model for the unmeasured confounder (\eqn{\alpha_X}). For a single
exposure term, a length-2 numeric \code{c(mean, sd)}. For a categorical
exposure with several terms, a matrix with one row \code{c(mean, sd)} per
exposure term, or a list of such length-2 vectors.}

\item{resid_sd}{Length-2 numeric giving the prior for the residual standard
deviation \eqn{\eta}. If \code{resid_dist = "uniform"} (the default) this is
\code{c(min, max)} of a uniform prior on \eqn{\eta}. If \code{resid_dist = "gamma"}
this is \code{c(shape, scale)} of a gamma prior on the residual \emph{precision}
\eqn{1/\eta^2}.}

\item{resid_dist}{Prior family for the residual standard deviation: either
\code{"uniform"} (on \eqn{\eta}) or \code{"gamma"} (on the precision \eqn{1/\eta^2}).}
}
\value{
An object of class \code{qba_u} describing a continuous unmeasured
confounder.
}
\description{
Describes the prior distributions of the bias parameters for a single
continuous unmeasured confounder, for use in \code{\link[=qbaconfound]{qbaconfound()}}.
}
\details{
The bias model relates the unmeasured confounder \code{U} to the study data
through three bias parameters: the coefficient of \code{U} in the outcome model
(\code{coef_out}, i.e. \eqn{\beta_U}), the coefficient(s) of the exposure in the
model for \code{U} (\code{coef_exp}, i.e. \eqn{\alpha_X}), and the residual standard
deviation of \code{U} given the exposure (\code{resid_sd}, i.e. \eqn{\eta}). Values
for these parameters cannot be estimated from the data and so are drawn from
the prior distributions specified here.
}
\examples{
u_continuous(coef_out = c(0.8, 0.1), coef_exp = c(0.3, 0.05),
             resid_sd = c(0.9, 1.1))
}
\seealso{
\code{\link[=u_binary]{u_binary()}}, \code{\link[=qbaconfound]{qbaconfound()}}
}
