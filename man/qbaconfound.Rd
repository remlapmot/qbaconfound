% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/qbaconfound.R
\name{qbaconfound}
\alias{qbaconfound}
\title{Monte Carlo quantitative bias analysis for unmeasured confounding}
\usage{
qbaconfound(
  formula,
  data,
  exposure = NULL,
  confounders,
  family = stats::gaussian(),
  reps = 1000L,
  sampling_error = TRUE,
  seed = NULL
)
}
\arguments{
\item{formula}{The naive analysis model, e.g. \code{y ~ x + c1 + c2} for a GLM or
\code{Surv(time, status) ~ x + c1} for a Cox model. The unmeasured confounders
are \emph{not} included in \code{formula}.}

\item{data}{A data frame containing the outcome, exposure, and measured
confounders.}

\item{exposure}{Character vector naming the exposure term(s) in \code{formula}
whose effect is of interest. Defaults to the first term on the right-hand
side.}

\item{confounders}{A single \code{\link[=u_continuous]{u_continuous()}}/\code{\link[=u_binary]{u_binary()}} object, or a list
of them, describing the unmeasured confounder(s).}

\item{family}{The outcome model family: a \code{\link[stats:glm]{stats::glm()}} family object,
a family name, or \code{"cox"} for a Cox proportional hazards model. Ignored
(and inferred as Cox) when the response is a \code{\link[survival:Surv]{survival::Surv()}} call.}

\item{reps}{Number of Monte Carlo replications.}

\item{sampling_error}{Logical; if \code{TRUE} (the default) Monte Carlo sampling
error is incorporated at step 4 above. Set to \code{FALSE} to obtain the
distribution of bias-adjusted point estimates without sampling error.}

\item{seed}{Optional integer seed for reproducibility.}
}
\value{
An object of class \code{qbaconfound}, a list with elements including
\code{estimates} (a data frame of naive and bias-adjusted estimates per exposure
term), \code{draws} (the matrix of bias-adjusted estimates across replications),
and \code{n_failed} (the number of replications whose model fit failed).
}
\description{
Conducts the flexible Monte Carlo quantitative bias analysis (QBA) for
unmeasured confounding of Hughes et al. Given a naive analysis model (which
omits one or more unmeasured confounders) and informative priors for a small
number of bias parameters, the function returns a bias-adjusted estimate of
the exposure effect together with an interval that accounts for both the
unmeasured confounding and sampling variability.
}
\details{
The substantive analysis may be a generalised linear model (any \code{\link[stats:glm]{stats::glm()}}
\code{family}) or a Cox proportional hazards model. Survival outcomes are detected
automatically when the left-hand side of \code{formula} is a \code{\link[survival:Surv]{survival::Surv()}}
call, or \code{family} can be set to \code{"cox"}.

Each Monte Carlo replication (see Hughes et al., section 2.4):
\enumerate{
\item draws a value for every bias parameter from its prior (the priors are
specified through \code{\link[=u_continuous]{u_continuous()}} / \code{\link[=u_binary]{u_binary()}});
\item simulates a proxy for each unmeasured confounder as a function of the
exposure only (a continuous proxy from a normal model, a binary proxy from
a Bernoulli model whose intercept reproduces the drawn prevalence);
\item refits the outcome model including the simulated proxies, with their
coefficients fixed to the drawn values (implemented as a model offset),
and reads off the exposure coefficient and its standard error;
\item adds Monte Carlo sampling error by drawing the bias-adjusted estimate
from a normal distribution centred on that coefficient.
}
The point estimate is the median and the interval the 2.5th and 97.5th
percentiles of the resulting distribution of bias-adjusted estimates.
}
\examples{
df <- sim_confounding(n = 500, beta_x = 0, seed = 1)

# Naive model y ~ x + c1 omits the confounder u; adjust for one continuous U.
fit <- qbaconfound(
  y ~ x + c1, data = df, exposure = "x",
  confounders = u_continuous(coef_out = c(0.8, 0.1),
                             coef_exp = c(0.6, 0.05),
                             resid_sd = c(0.9, 1.1)),
  reps = 200, seed = 1
)
fit
}
\references{
Hughes RA, Kawabata E, Palmer TM, et al. A flexible Monte Carlo quantitative
bias analysis for unmeasured confounding. \emph{Statistical Methods in Medical
Research} (under review).
}
\seealso{
\code{\link[=u_continuous]{u_continuous()}}, \code{\link[=u_binary]{u_binary()}}, \code{\link[=sim_confounding]{sim_confounding()}}
}
