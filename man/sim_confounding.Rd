% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/sim_confounding.R
\name{sim_confounding}
\alias{sim_confounding}
\title{Simulate data with an unmeasured confounder}
\usage{
sim_confounding(
  n = 1000L,
  beta_x = 0,
  family = c("gaussian", "binomial"),
  seed = NULL
)
}
\arguments{
\item{n}{Number of observations.}

\item{beta_x}{True exposure effect (on the linear-predictor scale).}

\item{family}{Outcome type: \code{"gaussian"} (continuous outcome) or
\code{"binomial"} (binary outcome).}

\item{seed}{Optional integer seed for reproducibility.}
}
\value{
A data frame with columns \code{y}, \code{x}, \code{c1}, and \code{u} (the unmeasured
confounder, included so it can be removed to mimic the unmeasured case).
The true exposure effect is stored in \code{attr(, "beta_x")}.
}
\description{
A small helper that simulates a dataset with one measured confounder \code{c1}
and one unmeasured confounder \code{u} that jointly confound the exposure-outcome
relationship. The naive model \code{y ~ x + c1} (which omits \code{u}) is therefore
biased for the exposure effect, making the data useful for examples and
tests of \code{\link[=qbaconfound]{qbaconfound()}}.
}
\examples{
df <- sim_confounding(n = 1000, beta_x = 0.5, seed = 42)
# Naive (biased) versus full (unbiased) model:
coef(lm(y ~ x + c1, df))["x"]
coef(lm(y ~ x + c1 + u, df))["x"]
}
